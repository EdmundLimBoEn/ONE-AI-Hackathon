# HUMANS

Actions that need a person, not the agent.

- [ ] Run `wrangler login` in an interactive terminal, then `bun run deploy`. Wrangler OAuth is expired and this environment is non-interactive, so the security fixes are committed and pushed but not yet on workers.dev.
- [ ] Confirm the production OpenRouter account has an operator-set spend or usage ceiling before paid models or sustained public traffic. The app now has a deployment-wide work-unit budget, but the provider dashboard cap is still independent.
